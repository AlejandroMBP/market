<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Cajas</h2>@can('cajas.crear')<a href="{{ route('cajas.create') }}" class="inline-flex items-center px-4 py-2 bg-gray-800 rounded-md font-semibold text-xs text-white uppercase">Abrir caja</a>@endcan</div></x-slot>
    <div class="py-12"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8">@if (session('success'))<div class="mb-4 rounded-md bg-green-50 p-4 text-sm text-green-700">{{ session('success') }}</div>@endif<div class="bg-white shadow-sm sm:rounded-lg"><div class="p-6 overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200"><thead><tr><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Usuario</th><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Apertura</th><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Cierre</th><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Inicial</th><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Final</th><th class="px-4 py-3 text-left text-xs text-gray-500 uppercase">Estado</th><th class="px-4 py-3 text-right text-xs text-gray-500 uppercase">Acciones</th></tr></thead>
        <tbody class="divide-y divide-gray-200">@forelse ($cajas as $caja)<tr><td class="px-4 py-3 text-sm">{{ $caja->user->name }}</td><td class="px-4 py-3 text-sm">{{ $caja->fecha_apertura->format('d/m/Y H:i') }}</td><td class="px-4 py-3 text-sm">{{ $caja->fecha_cierre?->format('d/m/Y H:i') ?? '-' }}</td><td class="px-4 py-3 text-sm">{{ number_format($caja->monto_inicial, 2) }}</td><td class="px-4 py-3 text-sm">{{ $caja->monto_final ? number_format($caja->monto_final, 2) : '-' }}</td><td class="px-4 py-3 text-sm">{{ $caja->estado }}</td><td class="px-4 py-3 text-right text-sm"><div class="flex justify-end gap-3"><a href="{{ route('cajas.show', $caja) }}" class="text-indigo-600">Ver</a>@can('cajas.editar')<a href="{{ route('cajas.edit', $caja) }}" class="text-yellow-600">Editar</a>@endcan</div></td></tr>@empty<tr><td colspan="7" class="px-4 py-6 text-center text-sm text-gray-500">No hay cajas registradas.</td></tr>@endforelse</tbody></table>
        <div class="mt-6">{{ $cajas->links() }}</div>
    </div></div></div></div>
</x-app-layout>
